/**
 * ScatterPilot Frontend Configuration
 * Auto-generated by configure-frontend.sh
 * Generated at: 2025-12-11 12:48:13 UTC
 */

const ScatterPilotConfig = {
    // API Configuration
    apiUrl: 'https://tph89c0qhc.execute-api.us-east-1.amazonaws.com/staging',
    environment: 'staging',

    // Feature Flags
    features: {
        demoMode: true,           // Keep demo mode available
        liveMode: true,            // Enable live API mode
        defaultMode: 'demo'        // Start in demo mode by default
    },

    // AWS Configuration
    aws: {
        region: 'us-east-1',
        accountId: '373345867629'
    },

    // Deployment Info
    deployment: {
        stackName: 'scatterpilot-vm-staging',
        deployedAt: '2025-12-11 12:47:42 UTC',
        version: '1.0.0'
    },

    // API Endpoints
    endpoints: {
        conversation: '/conversation',
        createInvoice: '/invoices',
        listInvoices: '/invoices',
        getInvoice: '/invoices/{invoice_id}',
        generatePDF: '/invoices/{invoice_id}/pdf'
    },

    // Settings
    settings: {
        maxRetries: 3,
        timeout: 30000,           // 30 seconds
        rateLimit: {
            maxRequests: 100,
            windowSeconds: 3600
        }
    }
};

// Export for use in other modules
if (typeof module !== 'undefined' && module.exports) {
    module.exports = ScatterPilotConfig;
}
