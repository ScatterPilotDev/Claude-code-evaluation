import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { PRICING } from '../config';
import apiService from '../services/api';
import authService from '../services/auth';
import analytics from '../utils/analytics';

export default function Pricing() {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');
  const [subscription, setSubscription] = useState(null);
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  const [openFaq, setOpenFaq] = useState(null);

  useEffect(() => {
    checkAuth();
    analytics.trackPricingPageViewed();
  }, []);

  const checkAuth = async () => {
    const authenticated = await authService.isAuthenticated();
    setIsAuthenticated(authenticated);

    if (authenticated) {
      try {
        const sub = await apiService.getSubscription();
        setSubscription(sub);
      } catch (err) {
        console.error('Failed to fetch subscription:', err);
      }
    }
  };

  const handleUpgrade = async () => {
    if (!isAuthenticated) {
      navigate('/');
      return;
    }

    analytics.trackUpgradeClicked('pricing_page');
    analytics.trackCheckoutStarted();

    setLoading(true);
    setError('');

    try {
      const successUrl = `${window.location.origin}/success`;
      const cancelUrl = `${window.location.origin}/pricing`;
      const response = await apiService.createCheckoutSession(successUrl, cancelUrl);

      if (!response?.url) {
        throw new Error('Invalid response from server: missing checkout URL.');
      }

      window.location.href = response.url;
    } catch (err) {
      setError(`Checkout Error: ${err.message || 'Failed to start checkout. Please try again.'}`);
      setLoading(false);
    }
  };

  const isPro = subscription?.subscription_status === 'pro';

  const toggleFaq = (index) => {
    setOpenFaq(openFaq === index ? null : index);
  };

  const freeFeatures = [
    '5 invoices per month',
    'AI-powered generation (30 seconds per invoice)',
    'Professional INVOICE header',
    'Add your contact information',
    'PDF download',
    'Email support'
  ];

  const proFeatures = [
    'UNLIMITED invoices',
    'Custom business name on invoices',
    '5 professional color themes',
    'No "Generated by ScatterPilot" watermark',
    'Priority email support (24hr response)',
    'Custom branding',
    'Professional appearance',
    'Faster support'
  ];

  const faqs = [
    {
      question: 'What happens if I exceed my free tier limit?',
      answer: "You'll need to wait until the next month or upgrade to Pro for unlimited invoices. Your limit resets on the 1st of each month."
    },
    {
      question: 'Do I need a credit card to try Pro?',
      answer: 'No! Start with 5 free invoices on our Free plan. Upgrade to Pro anytime — only $18/month with no contract.'
    },
    {
      question: 'Can I cancel anytime?',
      answer: "Yes, cancel anytime with one click. No questions asked, no hidden fees. You'll keep Pro access until the end of your billing period."
    },
    {
      question: 'What payment methods do you accept?',
      answer: 'We accept all major credit cards including Visa, Mastercard, and American Express. Payments are securely processed by Stripe.'
    }
  ];

  return (
    <div className="min-h-screen bg-surface-bg py-12 px-4 sm:px-6 lg:px-8">
      <div className="max-w-5xl mx-auto">
        {/* Hero */}
        <div className="text-center mb-14">
          <h1 className="text-display font-bold text-ink-primary mb-4">
            Simple, Transparent Pricing
          </h1>
          <p className="text-body-lg text-ink-secondary">
            Choose the plan that works for your business
          </p>
        </div>

        {error && (
          <div className="mb-8 p-4 bg-danger-50 border border-danger-200 rounded-card text-danger-700 text-center">
            {error}
          </div>
        )}

        {/* Pricing Cards */}
        <div className="grid md:grid-cols-2 gap-8 mb-14">
          {/* Free Plan */}
          <div className="bg-surface-card rounded-card shadow-card border border-surface-border p-8">
            <div className="text-center mb-8">
              <h2 className="text-heading-lg font-bold text-ink-primary mb-2">Free</h2>
              <div className="mt-4">
                <span className="text-5xl font-bold text-ink-primary">$0</span>
                <span className="text-body text-ink-tertiary">/month</span>
              </div>
              <p className="mt-3 text-body-sm text-ink-tertiary">Perfect for getting started</p>
            </div>

            <ul className="space-y-3 mb-8">
              {freeFeatures.map((feature, index) => (
                <li key={index} className="flex items-start">
                  <svg className="w-5 h-5 text-sage-500 mr-3 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2.5" d="M5 13l4 4L19 7" />
                  </svg>
                  <span className="text-body text-ink-secondary">{feature}</span>
                </li>
              ))}
            </ul>

            <button
              disabled={true}
              className="w-full py-3 px-6 rounded-button font-semibold text-body bg-surface-bg text-ink-tertiary cursor-not-allowed border border-surface-border"
            >
              {isPro ? 'Previous Plan' : 'Current Plan'}
            </button>
          </div>

          {/* Pro Plan */}
          <div className="bg-surface-card rounded-card shadow-modal border-2 border-sage-400 relative p-8">
            <div className="absolute -top-4 left-1/2 -translate-x-1/2">
              <span className="bg-sage-500 text-white px-5 py-1.5 rounded-pill text-body-sm font-bold shadow">
                Most Popular
              </span>
            </div>

            <div className="text-center mb-8 mt-2">
              <h2 className="text-heading-lg font-bold text-ink-primary mb-2">Pro</h2>
              <div className="mt-4">
                <span className="text-5xl font-bold text-sage-500">${PRICING.pro.price}</span>
                <span className="text-body text-ink-tertiary">/month</span>
              </div>
              <p className="mt-3 text-body-sm text-ink-tertiary font-medium">Most popular for consultants billing 6+ clients</p>
            </div>

            <ul className="space-y-3 mb-8">
              {proFeatures.map((feature, index) => (
                <li key={index} className="flex items-start">
                  <svg className="w-5 h-5 text-sage-500 mr-3 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2.5" d="M5 13l4 4L19 7" />
                  </svg>
                  <span className={`text-body text-ink-secondary ${index === 0 ? 'font-bold' : ''}`}>{feature}</span>
                </li>
              ))}
            </ul>

            {isPro ? (
              <button
                disabled={true}
                className="w-full py-3 px-6 rounded-button font-semibold text-body bg-success-50 text-success-700 cursor-not-allowed border border-success-200"
              >
                ✓ Current Plan
              </button>
            ) : (
              <button
                onClick={handleUpgrade}
                disabled={loading}
                className="w-full py-3 px-6 rounded-button font-bold text-body text-white bg-sage-500 hover:bg-sage-600 transition-colors shadow-card disabled:opacity-50 disabled:cursor-not-allowed"
              >
                {loading ? (
                  <span className="flex items-center justify-center">
                    <svg className="animate-spin -ml-1 mr-3 h-5 w-5 text-white" fill="none" viewBox="0 0 24 24">
                      <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                      <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
                    </svg>
                    Processing…
                  </span>
                ) : (
                  'Upgrade to Pro →'
                )}
              </button>
            )}

            {!isPro && (
              <p className="mt-3 text-center text-body-sm text-ink-tertiary">
                Start free today. Upgrade when you need unlimited invoices.
              </p>
            )}
          </div>
        </div>

        {/* Usage Info */}
        {subscription && !isPro && (
          <div className="mb-14 p-6 bg-surface-card rounded-card shadow-card text-center border border-surface-border">
            <p className="text-body text-ink-secondary">
              You've used <span className="font-bold text-sage-500">{subscription.invoices_this_month}</span> of{' '}
              <span className="font-bold text-sage-500">{subscription.invoices_limit}</span> invoices this month.
              {subscription.invoices_remaining <= 2 && (
                <span className="text-amber-600 ml-2 font-semibold">
                  Upgrade to Pro for unlimited invoices!
                </span>
              )}
            </p>
          </div>
        )}

        {/* Why ScatterPilot */}
        <div className="mb-14 bg-surface-card rounded-card shadow-card p-10 border border-surface-border">
          <h3 className="text-heading-lg font-bold text-center mb-8 text-ink-primary">
            Why ScatterPilot?
          </h3>
          <div className="grid md:grid-cols-2 gap-8">
            <div className="space-y-3">
              <h4 className="text-body font-bold text-ink-secondary mb-3">Traditional invoicing:</h4>
              {[
                '20–30 minutes per invoice',
                'Manual calculations',
                'Formatting headaches',
                'Lost time = lost money'
              ].map((item, index) => (
                <div key={index} className="flex items-center text-ink-tertiary">
                  <span className="text-xl mr-3">✗</span>
                  <span className="text-body">{item}</span>
                </div>
              ))}
            </div>
            <div className="space-y-3">
              <h4 className="text-body font-bold text-sage-500 mb-3">ScatterPilot:</h4>
              {[
                '30 seconds per invoice',
                'Automatic calculations',
                'Professional formatting',
                'Save 100+ hours/year'
              ].map((item, index) => (
                <div key={index} className="flex items-center text-ink-secondary font-medium">
                  <span className="text-xl mr-3">✓</span>
                  <span className="text-body">{item}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Trust Signals */}
        <div className="mb-14 grid grid-cols-2 md:grid-cols-4 gap-4">
          {[
            { icon: '🔒', text: 'Secure payments via Stripe' },
            { icon: '📧', text: 'Email support for all users' },
            { icon: '💳', text: 'Cancel anytime, no contract' },
            { icon: '⚡', text: 'Instant activation' }
          ].map((item, index) => (
            <div key={index} className="bg-surface-card p-5 rounded-card shadow-card text-center border border-surface-border">
              <div className="text-3xl mb-2">{item.icon}</div>
              <p className="text-body-sm text-ink-secondary font-medium">{item.text}</p>
            </div>
          ))}
        </div>

        {/* Social Proof */}
        <div className="mb-14 bg-sage-900 rounded-card p-10 text-center">
          <h3 className="text-heading-lg font-bold text-white mb-8">
            Trusted by Freelancers & Consultants
          </h3>
          <div className="grid md:grid-cols-3 gap-8">
            <div>
              <div className="text-4xl font-bold text-white mb-1">2,800+</div>
              <p className="text-body text-sage-200">Invoices created</p>
            </div>
            <div>
              <div className="text-4xl font-bold text-white mb-1">500+</div>
              <p className="text-body text-sage-200">Hours saved</p>
            </div>
            <div>
              <div className="text-4xl font-bold text-white mb-1">4.8/5</div>
              <p className="text-body text-sage-200">Average rating</p>
            </div>
          </div>
        </div>

        {/* FAQ */}
        <div className="mb-14">
          <h3 className="text-display font-bold text-ink-primary text-center mb-8">
            Frequently Asked Questions
          </h3>
          <div className="space-y-3">
            {faqs.map((faq, index) => (
              <div key={index} className="bg-surface-card rounded-card shadow-card overflow-hidden border border-surface-border hover:border-sage-300 transition-colors">
                <button
                  onClick={() => toggleFaq(index)}
                  className="w-full p-6 text-left flex items-center justify-between hover:bg-sage-50 transition-colors"
                >
                  <h4 className="font-semibold text-ink-primary text-body pr-4">
                    {faq.question}
                  </h4>
                  <svg
                    className={`w-5 h-5 text-sage-500 flex-shrink-0 transition-transform duration-300 ${openFaq === index ? 'rotate-180' : ''}`}
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 9l-7 7-7-7" />
                  </svg>
                </button>
                {openFaq === index && (
                  <p className="px-6 pb-6 text-body text-ink-secondary leading-relaxed">
                    {faq.answer}
                  </p>
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Back link */}
        <div className="text-center">
          <button
            onClick={() => navigate(isAuthenticated ? '/app' : '/')}
            className="text-sage-500 hover:text-sage-600 font-semibold text-body transition-colors"
          >
            ← {isAuthenticated ? 'Back to App' : 'Back to Home'}
          </button>
        </div>
      </div>
    </div>
  );
}
